# v5.4.2 TypeScript build fix

- Fixed the quote Discord notification payload so `customerEmail` is optional.
- Guest quote flows can legitimately omit email in some paths; the notification formatter already displays `Not provided` when absent.
- This resolves Vercel's `string | undefined` not assignable to `string` error in quote reply, quote acceptance, initial quote submission, and guest SMS reply code paths.
- No feature, database, or environment-variable changes.
