# v5.1.4 — Account navigation + logout placement

- Replaced the signed-in header **Logout** action with an **Account** link.
- Added the same **Account** link to the mobile navigation.
- Moved **Log Out** to the customer Account page, beside the account action buttons.
- Logout still uses the existing `/api/auth/logout` endpoint and returns the customer to the homepage.
