CAR DASH DETAILING - FULL REBUILD

What changed:
- Removed the duplicate homepage header and duplicate footer. The site now uses ONE global header and ONE global footer.
- Removed the hard-coded Basic / Premium / Ultimate packages from the public site.
- Public service cards now come ONLY from the Service database.
- Owner Dashboard > Services can create, edit, and delete packages.
- Every package has ONE price only. No separate Drop-Off / Delivery / Mobile price columns.
- Every public package gets its own Book Now button automatically.
- Added clearer success/error feedback in Owner Services.
- Updated service API authorization to accept the owner role or configured owner email.
- Homepage service preview is dynamic.
- Services page is dynamic.
- Reviews page now loads approved database reviews instead of fake hard-coded testimonials.
- Gallery remains controlled by Owner Dashboard uploads.
- Redesigned public site around a stronger black / red / warm-white theme.
- Rewrote public wording to sound more direct and natural for Car Dash Detailing.

IMPORTANT:
Replace the existing repo folder named `car-dash-website` with the `car-dash-website` folder in this ZIP.
Do NOT put the new folder inside the old `car-dash-website` folder.

No Prisma schema change is required for the new service setup. Existing Vercel database variables can stay the same.
