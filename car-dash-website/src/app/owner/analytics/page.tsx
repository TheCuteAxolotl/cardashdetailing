import { prisma } from "@/lib/prisma";
export const dynamic="force-dynamic";
export default async function Analytics(){const [bookings,quotes,accepted,users]=await Promise.all([prisma.booking.findMany({select:{status:true,createdAt:true}}),prisma.quoteThread.count(),prisma.quoteThread.findMany({where:{status:"accepted",quotedPrice:{not:null}},select:{quotedPrice:true}}),prisma.user.count({where:{role:"user"}})]);const revenue=accepted.reduce((s,x)=>s+(x.quotedPrice||0),0);const completed=bookings.filter(b=>b.status==='completed').length;const pending=bookings.filter(b=>b.status==='pending').length;const cards=[['Total bookings',bookings.length],['Pending requests',pending],['Completed',completed],['Quote chats',quotes],['Customers',users],['Accepted quote value',`$${revenue.toLocaleString()}`]];return <main className="min-h-screen bg-[#070707] px-6 py-10 text-white"><div className="mx-auto max-w-7xl"><div className="flex justify-between"><div><p className="text-xs uppercase tracking-[.28em] text-red-400">Owner</p><h1 className="mt-2 text-4xl font-semibold">Analytics</h1><p className="mt-2 text-white/40">A lightweight view of bookings, leads, customers, and accepted quote value.</p></div><a href="/owner/dashboard" className="h-fit rounded-full border border-white/15 px-5 py-3 text-sm">Back</a></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{cards.map(([k,v])=><div key={String(k)} className="rounded-[26px] border border-white/10 bg-white/[.03] p-6"><p className="text-sm text-white/40">{k}</p><p className="mt-3 text-4xl font-semibold">{v}</p></div>)}</div><p className="mt-6 text-xs text-white/25">Revenue here is based on accepted specialist quotes only. Stripe/payment reporting can be connected later.</p></div></main>}
