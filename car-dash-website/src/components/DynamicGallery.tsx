"use client";
import { useEffect,useMemo,useState } from "react";
type Img={id:string;url:string;title:string;category:string};
export default function DynamicGallery({limit}:{limit?:number}){const[images,setImages]=useState<Img[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{fetch('/api/images',{cache:'no-store'}).then(r=>r.ok?r.json():[]).then(d=>setImages(Array.isArray(d)?d:[])).finally(()=>setLoading(false))},[]);const visible=useMemo(()=>{const ok=new Set(['gallery','before-after','portfolio']);const f=images.filter(i=>ok.has(i.category));return typeof limit==='number'?f.slice(0,limit):f},[images,limit]);if(loading)return <div className="h-72 animate-pulse rounded-[24px] bg-white/5"/>;if(!visible.length)return <div className="rounded-[24px] border border-dashed border-white/15 p-8 text-sm text-white/35">Gallery photos will show here after they’re added from the Owner Dashboard.</div>;return <div className="grid auto-rows-[220px] grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-3">{visible.map((img,i)=><article key={img.id} className={`group relative overflow-hidden rounded-[22px] ${i===0?'sm:row-span-2 lg:col-span-2 lg:row-span-2':''}`}><img src={img.url} alt={img.title} className="h-full w-full object-cover transition duration-700 group-hover:scale-[1.025]"/><div className="absolute inset-0 bg-gradient-to-t from-black/75 via-transparent to-transparent"/><div className="absolute bottom-0 left-0 right-0 p-5"><p className="text-[10px] uppercase tracking-[.22em] text-red-400">{img.category.replaceAll('-',' ')}</p><h3 className="mt-1 text-lg font-semibold text-white">{img.title}</h3></div></article>)}</div>}
